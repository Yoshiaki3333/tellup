<p></p>
<h2>リスト一覧</h2>
<% @lists.each do |list| %>
 <p></p>
 <td id="<%= list.id %>"><%= list.id %>.<%= link_to "#{list.name}（#{list.project_name}）", "/lists/#{list.id}/companies" %></td>
 <p></p>
<% end %>